'use client';
import { useCaptainSocket } from "@/providers/captain-socket-provider";

export default function DrivePage() {
  const socket = useCaptainSocket();

  return (
    <>
      Captain Dashboard is under construction.
    </>
  );
}